
// Main export file - maintains backward compatibility with existing imports
export { FoodService } from './FoodService';
export type { DryFoodFormData, WetFoodFormData } from './types';

// Optional: export validation and calculation classes if needed elsewhere
export { FoodValidations } from './validations';
export { FoodCalculations } from './calculations';